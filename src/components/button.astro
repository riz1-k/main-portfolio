---

const props = Astro.props
---

<button
  class={
    "bg-background-primary text-accent border border-accent px-4 py-2 rounded-[4px] hover:button-transition"
  }
  {...props}
/>
  {props.children}
</button>
